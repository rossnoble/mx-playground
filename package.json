{
  "name": "playground",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter todo-api dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
